name: release
on:
  push:
    branches: [ main ]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install Go
        uses: actions/setup-go@v2
        with:
          go-version: '^1.18.0'
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Test
        run: go test ./...
  determine-version:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Determine release
        uses: go-semantic-release/action@v1
        id: semrel
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          changelog-generator-opt: "emojis=true"
          dry: true
          changelog-file: changelog.txt
      - name: Determine release notes hash
        run: echo "release-hash=changelog-${{ hashFiles('changelog.txt') }}" >> $GITHUB_ENV
      - name: Cache release notes
        uses: actions/cache@v3
        id: cache-release-notes
        with:
          path: changelog.txt
          key: ${{ env.release-hash }}
    outputs:
      version: ${{ steps.semrel.outputs.version }}
      release-notes-cache-key: ${{ env.release-hash }}
  release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    needs: determine-version
    strategy:
      matrix:
        goos:
          - 'darwin'
          - 'linux'
          - 'windows'
        goarch:
          - 'amd64'
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Load release notes
        uses: actions/cache@v3
        id: cache-release-notes
        with:
          path: changelog.txt
          key: ${{ needs.determine-version.outputs.release-notes-cache-key }}
      - name: Get OS and arch info
        if: needs.determine-version.outputs.version != ''
        run: |
          GOOS=${{matrix.goos}}
          GOARCH=${{matrix.goarch}}
          BINARY_NAME=timetrack-$GOOS-$GOARCH
          echo "BINARY_NAME=$BINARY_NAME" >> $GITHUB_ENV
          echo "GOOS=$GOOS" >> $GITHUB_ENV
          echo "GOARCH=$GOARCH" >> $GITHUB_ENV
      - name: Install Go
        uses: actions/setup-go@v2
        with:
          go-version: '^1.18.0'
      - name: Build
        if: needs.determine-version.outputs.version != ''
        run: |
          CGO_ENABLED=0 go build -a -ldflags "-s -w -X 'main.version=v${{ needs.determine-version.outputs.version }}'" -o "$BINARY_NAME"
      - name: Release with Notes
        uses: softprops/action-gh-release@v1
        if: needs.determine-version.outputs.version != ''
        with:
          tag_name: v${{ needs.determine-version.outputs.version }}
          body_path: changelog.txt
          files: ${{env.BINARY_NAME}}
